package net.alureon.ircbutt.command;

import org.junit.Test;

public class GiveCommandTest {

    @Test
    public void testGiveHandler() {
        // no idea how to actually test this without mocking up a fake IRC channel.
/*        BotResponse response = new BotResponse();
        String[] cmd = {"give", "alureon", "test"};
        GiveCommand.handleGive(response,null, null, cmd);
        System.out.println(response.getIntention()); */
    }
}
